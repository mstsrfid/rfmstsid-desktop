name: rfid
description: Projekat za skolu.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  dart_mappable: ^4.1.0
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  flutter_libserialport: ^0.3.0
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.7
  dart_mappable_builder: ^4.1.0

flutter:
  uses-material-design: true
